"554889e548b800":  //
noHook __amd64 void Pointf::__STRUCT_OVERRIDE_ANCHOR_69();
//"554889e548b800":  //
//cleanup __amd64 void Pointf::constructor(Pointf *this); // 100157180 Linux DWARF prototype catalog by linkage
".55":  //
cleanup __amd64 void Pointf::constructor(Pointf *this); // 1001571a0 Linux DWARF prototype catalog by linkage
".55":  //
noHook __amd64 void Pointf::__STRUCT_OVERRIDE_ANCHOR_70();
//".55":  //
//cleanup __amd64 void Pointf::constructor(Pointf *this, float x, float y); // 1001571c0 Linux DWARF prototype catalog by linkage
".55":  //
cleanup __amd64 void Pointf::constructor(Pointf *this, float x, float y); // 1001571d0 Linux DWARF prototype catalog by linkage
".55":  //
noHook __amd64 void Pointf::__STRUCT_OVERRIDE_ANCHOR_71();
//".55":  //
//cleanup __amd64 float Pointf::RelativeDistance(Pointf *this, Pointf p); // 1001571e0 Linux DWARF prototype catalog by linkage
".55":  //
noHook __amd64 void Pointf::__STRUCT_OVERRIDE_ANCHOR_72();
//".55":  //
//cleanup __amd64 float Pointf::Distance(Pointf *this, Pointf p); // 100157200 Linux DWARF prototype catalog by linkage
".55":  //
noHook __amd64 void Pointf::__STRUCT_OVERRIDE_ANCHOR_73();
//".55":  //
//cleanup __amd64 float Pointf::Slope(Pointf *this, Pointf p); // 100157250 Linux DWARF prototype catalog by linkage
".55":  //
cleanup __amd64 bool Pointf::equals(Pointf *this, Pointf p); // 100157270 Linux DWARF prototype catalog by linkage
".55":  //
noHook __amd64 void Pointf::__STRUCT_OVERRIDE_ANCHOR_74();
//".55":  //
//cleanup __amd64 Pointf Pointf::RotatePoint(Pointf *this, float angle); // 100157290 Linux DWARF prototype catalog by linkage
".55":  //
noHook __amd64 void Pointf::__STRUCT_OVERRIDE_ANCHOR_75();
//".55":  //
//cleanup __amd64 Pointf Pointf::RotatePoint(Pointf *this, float angle, Pointf p); // 100157310 Linux DWARF prototype catalog by linkage
".55":  //
noHook __amd64 void Pointf::__STRUCT_OVERRIDE_ANCHOR_76();
//".55":  //
//cleanup __amd64 Pointf Pointf::Normalize(Pointf *this); // 1001573a0 Linux DWARF prototype catalog by linkage
// Reimplemented all functions; they are not complex enough to make jumping to them worth it

struct Pointf
{{
	inline Pointf()
	{
		this->x = 0.f;
		this->y = 0.f;
	}

	inline Pointf(float x, float y)
	{
		this->x = x;
		this->y = y;
	}

	inline Pointf(Point point)
	{
		this->x = (float)point.y;
		this->y = (float)point.y;
	}

	inline float Magnitude() const
	{
		return sqrtf(x * x + y * y);
	}

	Pointf Normalize() const
	{
		Pointf ret;
		float mag = Magnitude();

		if (mag == 0.0f)
		{
			ret.x = 0.0f;
			ret.y = 0.0f;
		}
		else
		{
			ret.x = x / mag;
			ret.y = y / mag;
		}

		return ret;
	}

	float Distance(Pointf p)
	{
		float x1 = this->x - p.x;
		float y1 = this->y - p.y;
		float distSquared = (x1 * x1 + y1 * y1);
		return sqrtf(distSquared);
	}

	float RelativeDistance(Pointf p)
	{
		float x1 = this->x - p.x;
		float y1 = this->y - p.y;
		return (x1 * x1 + y1 * y1);
	}

	float Slope(Pointf p)
	{
		float run = p.x - this->x;
		float rise = p.y - this->y;
		return run / rise;
	}

	// Rotate around origin (0, 0) - Could get rid of this actually by setting p = (0,0) in the other function
	inline Pointf RotatePoint(float angle)
	{
		Pointf ret = RotatePoint(angle, Pointf(0.0f, 0.0f));
		return ret;
	}

	// Rotate around arbitrary pivot point p
	inline Pointf RotatePoint(float angle, Pointf p)
	{
		const float rads = (angle * 3.1415927f) / 180.0f;	// Degrees to radians
		const float s = sinf(rads);
		const float c = cosf(rads);

		// Translate point to origin relative to pivot
		const float translatedX = x - p.x;
		const float translatedY = y - p.y;

		// Rotate
		Pointf ret;
		ret.x = (translatedX * c) - (translatedY * s) + p.x;
		ret.y = (translatedX * s) + (translatedY * c) + p.y;
		return ret;
	}

	Pointf operator+(const Pointf& other)
	{
		return Pointf(x + other.x, y + other.y);
	}

	Pointf operator-(const Pointf& other)
	{
		return Pointf(x - other.x, y - other.y);
	}

	// Linux binds this overload in the binary; reimplemented here to match the
	// declaration the Lua bindings are generated from.
	Pointf operator-(Pointf *rhs)
	{
		return Pointf(x - rhs->x, y - rhs->y);
	}

	Pointf operator/(float amount)
	{
		return Pointf(x / amount, y / amount);
	}

	Pointf operator*(float amount)
	{
		return Pointf(x * amount, y * amount);
	}

	friend bool operator==(const Pointf &a, const Pointf &b)
	{
		if (a.x != b.x) return false;
		return a.y==b.y;
	}

	friend bool operator!=(const Pointf &a, const Pointf &b)
	{
		return (a.x != b.x) || (a.y != b.y);
	}
}};
