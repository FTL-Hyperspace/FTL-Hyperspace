"NOT FOUND IN SIGNATURE DATA":
cleanup __amd64 void DamageMessage::constructor(DamageMessage *this, float length, Pointf pos, DamageMessage::MessageType type);
"NOT FOUND IN SIGNATURE DATA":
cleanup __amd64 void DamageMessage::constructor2(DamageMessage *this, float length, int amount, Pointf pos, bool unk);


struct DamageMessage 
{{

	enum MessageType
	{
	  MISS = 0x0,
	  RESIST = 0x1,
	};

	DamageMessage(float length, Pointf pos, MessageType type)
	{
		constructor(length, pos, type);
	}
	
	DamageMessage(float length, int amount, Pointf pos, bool unk)
	{
		constructor2(length, amount, pos, unk);
	}
}};
