#include "Global.h"
/*
static bool first = true;

HOOK_GLOBAL(random32, () -> unsigned int)
{
    if (first)
    {
        *G_->randomState = 5;
        first = false;
    }
    unsigned int ret = super();

    printf("random_state: %llu   ret: %u\n", *G_->randomState, ret);

    return ret;
}
*/
