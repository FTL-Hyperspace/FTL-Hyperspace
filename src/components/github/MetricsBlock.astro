---
/**
 * Wraps page content built around live GitHub numbers. Takes no space until the
 * numbers arrive and never appears without them (.gh-collapsed in global.css).
 */
---

<div class="gh-collapsed">
	<slot />
</div>
