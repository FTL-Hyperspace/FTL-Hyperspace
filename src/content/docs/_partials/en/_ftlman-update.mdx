import { Aside } from '@astrojs/starlight/components';

<Aside type="tip" title="ftlman can do this for you">
ftlman has a built-in Hyperspace installer. Configure it in ftlman's update settings
and it fetches and applies new versions itself, so you can skip the file swap above.
</Aside>
