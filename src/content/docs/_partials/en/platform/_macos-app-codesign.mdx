1. Open **Terminal**.
2. Paste this in, then press the space bar:

   ```sh
   codesign -f -s - --timestamp=none --all-architectures --deep
   ```

3. Drag `FTL.app` from Finder into the Terminal window. Its full path is filled in
   for you, so there is nothing to type.
4. Press **Return**.
