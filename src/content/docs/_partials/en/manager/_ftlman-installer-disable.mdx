import Screenshot from '~/components/Screenshot.astro';
import disableInstaller from '~/assets/screenshot/ftlman-disable-installer.png';

Turn off ftlman's own Hyperspace installer, which would otherwise overwrite the
files you installed by hand: in **Settings**, open **Advanced settings** and
tick **Disable Hyperspace installer**.

<Screenshot src={disableInstaller} alt="ftlman's Settings window with Disable Hyperspace installer ticked under Advanced settings" label="Show me the setting" />
