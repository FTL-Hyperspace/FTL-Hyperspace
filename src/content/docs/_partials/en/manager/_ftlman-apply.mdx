import SelectRelease from "@p/manager/_ftlman-hyperspace-select.mdx";
import SupplyFile from "@p/manager/_ftlman-hyperspace-file-manual.mdx";
import DisableInstaller from "@p/manager/_ftlman-installer-disable.mdx";
import OverrideNote from "@p/shared/_hyperspace-override.mdx";

{/* With the prop.manual set, extra steps cover a hand-installed Hyperspace. */}

<ol>
	{props.manual && <li><DisableInstaller /></li>}
	<li>{props.manual ? <SupplyFile /> : <SelectRelease />}</li>
	<li>Put your mods in the ftlman's `mods` folder.</li>
	<li>Press **Scan** in the top right so ftlman can find them.</li>
	{props.manual && <li>
		Drag `Hyperspace.ftl` to the top of the list. Check the order, top to bottom:
		<ol>
			<li>`Hyperspace.ftl`</li>
			<li>Your other mods</li>
		</ol>
	</li>}
	<li>
		Tick mods you want to use. Order matters, some mods specify that they must
		be loaded before (e.g. Multiverse Assets). To change order simply drag the
		mod up or down in the list.

    	{props.manual && <OverrideNote />}
    </li>
    <li>Press **Apply**.</li>

</ol>
