import { Aside } from '@astrojs/starlight/components';
import Screenshot from '~/components/Screenshot.astro';
import properties from '~/assets/screenshot/steam-properties.png';

Right-click **FTL: Faster Than Light** in your library, open **Properties**, and under
**Updates** set automatic updates to **Only update this game when I launch it**.

<Screenshot src={properties} alt="Right-clicking FTL in the Steam library and choosing Properties" label="Show me how to open Properties" />

<Aside type="danger" title="Steam will undo your install">
If Steam updates FTL it replaces the patched game files, and Hyperspace stops loading.
</Aside>
