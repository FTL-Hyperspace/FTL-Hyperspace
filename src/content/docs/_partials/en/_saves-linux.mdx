import BackupSaves from './_backup-saves.mdx';

Your saves live in `~/.local/share/FasterThanLight`. Hidden folders are not shown by
default, so press **Ctrl-H** in your file manager, or paste the path into its location
bar.

<BackupSaves />
